
ion-view(title='{{title}}')
  ion-content(padding='true')
    h2 Welcome
    .list
        label(class='item item-input')
            span(class='input-label' ) Student ID
            input(type='text' ng-model="user.username")

        button(class="button button-block button-positive" ng-click="signIn(user)") Sign-In
